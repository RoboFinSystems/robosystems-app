import { HeroSection } from '@robosystems/core'

/** The full landing hero — gradient headline, value proposition, and the primary
 *  call-to-action. Full-bleed on a dark canvas; give the card a tall viewport so
 *  the section fills. */
export const Default = () => <HeroSection />
